[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name='JPyDB'
version='0.2'
description='A Database Mangager, this can storage multiples things'
dependencies = []